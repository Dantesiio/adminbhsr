# Local development pointing to Vercel Postgres
# Paste the "Prisma" connection string from Vercel Postgres into DATABASE_URL.
# For migrations (db:push/db:seed), also set DIRECT_DATABASE_URL to the "Direct" connection string.
# After switching to this file, run:
#   pnpm db:push
#   pnpm db:seed

# Example (replace with your values):
# DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require&pgbouncer=true&connection_limit=1"
# DIRECT_DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"  

DATABASE_URL="postgresql://<USER>:<PASS>@<HOST>:<PORT>/<DB>?sslmode=require&pgbouncer=true&connection_limit=1"
DIRECT_DATABASE_URL="postgresql://<USER>:<PASS>@<HOST>:<PORT>/<DB>?sslmode=require"

# Auth.js
AUTH_SECRET="development-secret-change-in-production"
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# Public app URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional services for dev
RESEND_API_KEY=""
BLOB_READ_WRITE_TOKEN=""
