# DATABASE (choose one)
# Local Docker (matches scripts/start-db.sh):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5433/adminbhsr"
#
# Vercel Postgres (Prisma connection string):
# DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require&pgbouncer=true&connection_limit=1"
# And for migrations push/seed use the Direct connection string via DIRECT_DATABASE_URL:
# DIRECT_DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"

DATABASE_URL="postgresql://postgres:postgres@localhost:5433/adminbhsr"
# DIRECT_DATABASE_URL=""

# AUTH (NextAuth)
AUTH_SECRET="your-secret-key-here"
AUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST="true"

# RESEND (emails)
RESEND_API_KEY="your-resend-api-key"

# VERCEL BLOB (for file storage)
BLOB_READ_WRITE_TOKEN="your-vercel-blob-token"

# S3 (alternative to Vercel Blob)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=

# NEXT_PUBLIC_APP_URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
